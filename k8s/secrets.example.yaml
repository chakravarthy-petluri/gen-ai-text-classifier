# IMPORTANT: This is an EXAMPLE file only!
# DO NOT commit actual API keys to the repository
# Create the actual secret using kubectl or cloud-specific secret management

# Option 1: Create secret with kubectl (recommended for development)
# kubectl create secret generic genai-text-classifier-secrets \
#   --from-literal=gemini-api-key='YOUR_GEMINI_API_KEY' \
#   --from-literal=openai-api-key='YOUR_OPENAI_API_KEY' \
#   -n default

# Option 2: Use this YAML template (AFTER filling in actual values)
# apiVersion: v1
# kind: Secret
# metadata:
#   name: genai-text-classifier-secrets
#   namespace: default
# type: Opaque
# stringData:
#   gemini-api-key: "YOUR_GEMINI_API_KEY"
#   openai-api-key: "YOUR_OPENAI_API_KEY"

# Option 3: Cloud-specific secret management (RECOMMENDED FOR PRODUCTION)
#
# Oracle Cloud - OCI Vault:
#   oci vault secret create --secret-name genai-gemini-key --secret-content "YOUR_KEY"
#   https://docs.oracle.com/en-us/iaas/Content/KeyManagement/Tasks/managingtypes.htm
#
# AWS - Secrets Manager:
#   aws secretsmanager create-secret --name genai-text-classifier-secrets \
#     --secret-string '{"gemini-api-key":"YOUR_KEY","openai-api-key":"YOUR_KEY"}'
#   https://docs.aws.amazon.com/secretsmanager/
#
# GCP - Secret Manager:
#   echo -n "YOUR_KEY" | gcloud secrets create gemini-api-key --data-file=-
#   https://cloud.google.com/secret-manager/docs
#
# Azure - Key Vault:
#   az keyvault secret set --vault-name MyKeyVault --name gemini-api-key --value "YOUR_KEY"
#   https://docs.microsoft.com/en-us/azure/key-vault/